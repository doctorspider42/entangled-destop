[package]
name = "virtio-block"
description = "virtio-blk device with a RAW file backend"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
publish.workspace = true

[dependencies]
# Hole punching / zeroing on the backing image: the host half of DISCARD and
# WRITE_ZEROES lives in disk-image::ops, which is portable by construction.
disk-image.workspace = true
thiserror.workspace = true
tracing.workspace = true
virtio-core.workspace = true
virtio-queue.workspace = true
vm-memory.workspace = true

[dev-dependencies]
virtio-core = { workspace = true, features = ["test-utils"] }

[lints]
workspace = true
