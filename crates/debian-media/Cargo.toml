[package]
name = "debian-media"
description = "Debian media resolution, download verification and provenance manifests"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true

[dependencies]
thiserror.workspace = true
serde.workspace = true
toml.workspace = true
sha2.workspace = true
hex.workspace = true

# The HTTP client (rustls-based) and OpenPGP verification (`rpgp`, MIT/Apache —
# NOT sequoia, which is LGPL) are added when the downloader lands (MVP-60x).

[lints]
workspace = true
