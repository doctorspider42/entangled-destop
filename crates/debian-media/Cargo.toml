[package]
name = "debian-media"
description = "Debian media resolution, download verification and provenance manifests"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
publish.workspace = true

[dependencies]
thiserror.workspace = true
tracing.workspace = true
serde.workspace = true
toml.workspace = true
sha2.workspace = true
hex.workspace = true
# rustls-based HTTP client: no OpenSSL linkage, static builds.
ureq.workspace = true
# OpenPGP verification via rpgp (MIT/Apache-2.0) — NOT sequoia, which is LGPL.
pgp.workspace = true

[dev-dependencies]
# Test-only OpenPGP key generation (rpgp 0.17 is built against rand 0.8).
rand.workspace = true

[lints]
workspace = true
